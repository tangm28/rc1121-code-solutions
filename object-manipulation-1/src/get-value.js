/* exported getValue */
// define a function named 'getValue' which takes 2 parameters, objects and key
// return the object with key in bracket notation
function getValue(object, key) {
  return object[key];
}
