/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// define a function named 'getLastChar' with a single parameter 'string'
// find the last index of a string/array which is the length subtracted by 1
// return the string variable at the last index
