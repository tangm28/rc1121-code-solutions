/* exported setValue */
// define a function named 'setValue' which takes 3 parameters, objects, key, and value
// assign it value to the object with key in bracket notation
function setValue(object, key, value) {
  object[key] = value;
}
